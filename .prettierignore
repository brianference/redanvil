# Machine-generated by the gate and the feed builder. Prettier reformats them
# differently from JSON.stringify, which breaks the byte-exact CI check that
# proves the dashboard feed was derived rather than hand-edited.
results/*.json
evidence/*.json

# Mirror of ~/.claude/skills/mobile-ux/references/mobile-design-rules.md.
# Verified byte-for-byte by .github/scripts/verify_design_rules.mjs, so any
# reformat here reads as drift from the source of truth.
design-system/mobile-design-rules.md
