# Typing lane (v1.0.0)

- u-typing-strict (blocker, det): mypy strict and tsc strict pass on changed code.
- u-typing-no-any (blocker, det): no `any` or `as any` in TS; no untyped defs in Python.
- u-typing-scoped-ignores (major, det): every suppression carries the specific code and a reason.
