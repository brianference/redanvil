/**
 * Which of the four wizard steps is showing: Prompt, Scope, Features, Review.
 * Wizard, Stepper and ComingUp all take this one type, so adding a step is a
 * single change here.
 */
export type WizardStepIndex = 1 | 2 | 3 | 4;
