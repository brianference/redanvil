import { Page } from '../components/Page';

/** Home page. Copy belongs in the locale bundle; kept inline here only as a compliant seed. */
export function Home(): JSX.Element {
  return (
    <Page title="Home">
      <p>Home content.</p>
    </Page>
  );
}
