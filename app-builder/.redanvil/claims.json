{
  "_why": "PROBE ONLY, and deliberately so. app-builder was hand-built and has no generated PRD, so there is nothing to transcribe feature claims from -- and hand-authoring them would be inventing claims rather than recording them. `features` is empty, so u-claims-covered correctly returns n/a here rather than grading this app against a list I made up. What IS recorded is how to exercise the primary flow, which is a test instruction, not a claim about capability.",
  "kind": "claims",
  "slug": "app-builder",
  "title": "RedAnvil",
  "appType": "dashboard",
  "hasAuth": false,
  "entities": [],
  "threshold": 90,
  "features": [],
  "design": {
    "_why": "Recorded retroactively by opening evidence/screenshots/app-builder-1280-dark.png and evidence/screenshots/app-builder-1280-light.png (both files actually opened and viewed, not inferred from source): the home route is a two-pane split -- a persistent composer (the work: describe your app, send) against a template/example picker -- which is a creation-tool shell, i.e. Command canvas, not one of the three archetypes exempt from the anti-column check. Re-open those two PNGs to check this call. Confirmed NOT a centred column by a separate machine measurement (design_audit.mjs --claims against production, evidence/design-app-builder.json): widest block 1306 of 1440px (91%), equal 67px gutters -- wide enough that the anti-column rule (requires <72% width AND equal gutters to fire) would not trigger regardless of archetype name.",
    "archetype": "Command canvas"
  },
  "coldVisitorProbe": {
    "control": "send description",
    "fill": {
      "#composer-prompt": "an app to track bird sightings with photos and dates"
    },
    "expectSelector": "button:has-text('Next')",
    "minCount": 1
  },
  "deployUrl": "https://redanvil.pages.dev"
}
